// SPDX-License-Identifier: GPL-3.0
pragma solidity {{ compilerVersion }};

contract {{ contractName }} {

    {{#variable}}
    {{{ variable }}}
    {{/variable}}

    constructor(address {{{ payable }}}recipient) public payable {
        {{{success}}}recipient.send(msg.value);
        
        {{#checkSuccess}}
        {{{ checkSuccess }}}
        {{/checkSuccess}}

        {{#assignVariable}}
        {{{ assignVariable }}}
        {{/assignVariable}}
    }
}